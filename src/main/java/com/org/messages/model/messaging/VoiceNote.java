package com.org.messages.model.messaging;

import com.org.messages.model.user.User;

public class VoiceNote extends ChatElement {
    public VoiceNote(int chatElementID, User sender) {
        super(chatElementID, sender);
    }
}
